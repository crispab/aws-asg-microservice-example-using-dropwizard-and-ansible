---

- hosts: localhost
  connection: local
  gather_facts: yes  # needed for ansible_date_time
  roles:
    - role: betrcode.aws_ecr_docker_push
    - role: betrcode.aws-asg
      docker_image: "{{ _tag }}" # Coming from previous role
